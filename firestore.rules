rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // Panel + HP pakai Anonymous Auth. Privat per kode pairing (path tidak bisa ditebak).
    match /{p=**} {
      allow read, write: if request.auth != null;
    }
  }
}
